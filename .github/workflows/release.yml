name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  publish:
    name: Build & Publish Release
    permissions:
      contents: write
    runs-on: windows-latest

    steps:
      - name: Checkout SnippetFlow
        uses: actions/checkout@v4

      - name: Checkout common_lib
        uses: actions/checkout@v4
        with:
          repository: tkshnkgwr/common_lib
          token: ${{ secrets.PAT || github.token }}
          path: common_lib_tmp

      - name: Move common_lib to parent directory
        shell: pwsh
        run: |
          Move-Item -Path common_lib_tmp -Destination ../common_lib

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Rust Cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: "src-tauri -> target"

      - name: Install dependencies
        run: npm ci

      - name: Build Tauri App and Publish Release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'SnippetFlow ${{ github.ref_name }}'
          releaseBody: 'SnippetFlow 最新リリースアセット (.msi / .exe)'
          releaseDraft: false
          prerelease: false
